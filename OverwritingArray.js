let array = [1, 2, 3, 4, 5];
let element = 10;
let position = 2; // Insert at index 2

// Overwrite the element at the specified index
array[position] = element;

console.log(array); // Output: [1, 2, 10, 4, 5]